/*
    //1. Crea una función llamada esPar que reciba un número como parámetro y 
    // devuelva true si el número es par o false si no lo es.

        function esPar(numero){
            return numero%2===0;
        } 
        console.log(esPar(3));
        console.log(esPar(5));
        console.log(esPar(2));
*/

/*
    //2. Implementa una función llamada 
    // convertirGrados que convierta grados Celsius a Fahrenheit.

        function convertirgrados(celsius) {
            return (celsius*9/5)+ 32;
        }
        console.log(convertirgrados(0));
        console.log(convertirgrados(100));
        console.log(convertirgrados(-30));
*/

/*
    //3. Crea una función llamada mayor que
    //  reciba dos números y devuelva el mayor de ellos.
    
        function mayor (num1, num2){
        if (num1>num2) {

            return num1;
        }else{
            return num2
        }
        }
        console.log (mayor(1, 2));
*/

/*
    //4. Escribe una función llamada cuadrado 
    // que reciba un número y devuelva su cuadrado.

        function cuadrado (num){
            return (num*num);
        }
        console.log(cuadrado(5));
*/

/*
    //5. Crea una función llamada areaTriangulo que reciba la base 
    // y la altura de un triángulo y devuelva su área.

        function areaTriangulo (base, altura){
            return (base * altura) /2;
        } 
        console.log(areaTriangulo(12,15));
*/

/*
    //6. Escribe una función llamada promedio
    //  que reciba tres números y devuelva su promedio.

        function promedio (num1, num2, num3){
            return(num1+num2+num3)/3;
            
        }
            console.log(promedio(10,20,30));
*/

/*
    //7. Crea una función llamada calcularAreaCirculo que reciba como parámetro 
    // el radio de un círculo y devuelva el área del círculo. 

        function calcularAreaCirculo(radio) {
            return Math.PI * Math.pow(radio, 2);
        }
        console.log(calcularAreaCirculo(5)); 
        console.log(calcularAreaCirculo(10));
*/

/*
    //8. Crea una función llamada calcularPerimetro que reciba como parámetros el 
    // largo y el ancho de un rectángulo y devuelva su perímetro.
    
        function calcularPerimetro(largo, ancho){
            return (largo+ancho) *2
        }
        console.log(calcularPerimetro(18,12));
*/


    //9. Crea una función llamada volumenCubo que reciba un 
    // lado del cubo y devuelva su volumen.
    
        function volumencubo (lado){
            return ( Math.pow(lado,3));
        }
        console.log(volumencubo(3));




    
